[build]
  command = "cd client && pnpm install && pnpm run build"
  publish = "client/dist"
  base = "client"

[build.environment]
  NODE_VERSION = "18"  # Match your local Node version
  PNPM_VERSION = "8"   # Match your pnpm version